<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yw.secondhandtrade.server.mapper.OrdersMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders (order_no, buyer_id, seller_id, total_amount, trade_location, consignee, contact_phone, status, create_time, update_time)
        values
            (#{orderNo}, #{buyerId}, #{sellerId}, #{totalAmount}, #{tradeLocation}, #{consignee}, #{contactPhone}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update orders
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="paymentTime != null">payment_time = #{paymentTime},</if>
            <if test="completionTime != null">completion_time = #{completionTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <select id="getById" resultType="Orders">
        select * from orders where id = #{id}
    </select>

    <select id="pageQuery" resultType="OrderVO">
        select * from orders
        <where>
            (buyer_id = #{userId} or seller_id = #{userId})
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="getByStatusAndCreateTimeLT" resultType="Orders">
        select * from orders where status = #{status} and create_time &lt; #{createTime}
    </select>

</mapper>
